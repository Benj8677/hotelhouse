{% extends 'base.html.twig' %}

{% block title %}{{ spa.titre }}{% endblock %}

{% block body %}

{% for message in app.flashes('success') %}
        <div class="alert alert-success text-center">{{ message }}</div>
{% endfor %}

<section class="row articles mx-auto">

    <article class="col-md-10 mx-auto text-center border border-dark my-3 p-3">
        <div class="content">
            <h2 class='text-warning fw-bold'>{{ spa.titre }}</h2>
            <p class="text-secondary">{{ spa.descShort|raw }}</p>
            <img src="{{ asset('img/service/' ~ spa.photo) }}" alt="" class="img-fluid me-2" style="max-width:400px;float:left;">
            <p class="text-secondary">{{ spa.descLong|raw }}</p>
        </div>
        {% if app.user %}
            {% if spa.nbPlace>0 %}
            {# <a href="{{ path('cart_add', {id:spa.id}) }}" class="btn btn-outline-success m-3">Réserver</a> #}
            {% else %}
                <span class="text-danger">spa indisponible</span>
            {% endif %}
        {% else %}
            <a href="{{ path('app_register') }}" class="btn btn-outline-success m-3">Créer un compte</a>
            <a href="#" class="btn btn-outline-success m-3" onclick="getElementById('connect').style.display='';getElementById('body').style='background-color: rgba(0, 0, 0, 0.4);';">Se connecter</a>
        {% endif %}
    </article>

</section>

{% endblock %}
